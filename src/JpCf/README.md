#### JpCf Module

It currently contains 2 parts, the frame_class, and the complex_class
the frame class is the class that manages the jupyter canvas, and the complex class is an instance of 
a class that the frame_class can use to generate the images!
